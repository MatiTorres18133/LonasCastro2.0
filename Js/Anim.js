window.addEventListener("scroll", function(){

    let animacion =this.document.getElementById("animado");
    let posticionObj1= animacion.getBoundingClientRect().top;
    console.log(posticionObj1);
    let tamañoDePantalla= window.innerHeight;


    if(posticionObj1 < tamañoDePantalla){
        animacion.style.animation="mover 1s ease-out"
    }

})
/*=========================IMAGEN2================================*/



window.addEventListener("scroll", function(){

    let animacion =this.document.getElementById("animado2");
    let posticionObj1= animacion.getBoundingClientRect().top;
    console.log(posticionObj1);
    let tamañoDePantalla= window.innerHeight;


    if(posticionObj1 < tamañoDePantalla){
        animacion.style.animation="mover2 1s ease-out"
    }

})


/*=========================IMAGEN3================================*/
window.addEventListener("scroll", function(){

    let animacion =this.document.getElementById("animado3");
    let posticionObj1= animacion.getBoundingClientRect().top;
    console.log(posticionObj1);
    let tamañoDePantalla= window.innerHeight;


    if(posticionObj1 < tamañoDePantalla){
        animacion.style.animation="mover3 1s ease-out"
    }

})



/*=========================IMAGEN-INDEX================================*/
window.addEventListener("scroll", function(){

    let animacion =this.document.getElementById("text-primary");
    let posticionObj1= animacion.getBoundingClientRect().top;
    console.log(posticionObj1);
    let tamañoDePantalla= window.innerHeight;


    if(posticionObj1 < tamañoDePantalla){
        animacion.style.animation="mover-text-primary 0.5s ease-out"
    }

})


window.addEventListener("scroll", function(){

    let animacion =this.document.getElementById("carouselExampleIndicators2");
    let posticionObj1= animacion.getBoundingClientRect().top;
    console.log(posticionObj1);
    let tamañoDePantalla= window.innerHeight;


    if(posticionObj1 < tamañoDePantalla){
        animacion.style.animation="mover-carrousel 1s ease-out"
    }

})


window.addEventListener("scroll", function(){

    let animacion =this.document.getElementById("TXT-INDEX");
    let posticionObj1= animacion.getBoundingClientRect().top;
    console.log(posticionObj1);
    let tamañoDePantalla= window.innerHeight;


    if(posticionObj1 < tamañoDePantalla){
        animacion.style.animation="mover-txt-index 0.5s ease-out"
    }

})



window.addEventListener("scroll", function(){

    let animacion =this.document.getElementById("BTN-ACERC");
    let posticionObj1= animacion.getBoundingClientRect().top;
    console.log(posticionObj1);
    let tamañoDePantalla= window.innerHeight;


    if(posticionObj1 < tamañoDePantalla){
        animacion.style.animation="mover-btn-acerc 0.5s ease-out"
    }

})






/*=========================MOVER-TRABAJOS-INDEX================================*/
window.addEventListener("scroll", function(){

    let animacion =this.document.getElementById("cardsIMG");
    let posticionObj1= animacion.getBoundingClientRect().top;
    console.log(posticionObj1);
    let tamañoDePantalla= window.innerHeight;


    if(posticionObj1 < tamañoDePantalla){
        animacion.style.animation="mover-card1 1s ease-out"
    }

})


window.addEventListener("scroll", function(){

    let animacion =this.document.getElementById("cardsIMG2");
    let posticionObj1= animacion.getBoundingClientRect().top;
    console.log(posticionObj1);
    let tamañoDePantalla= window.innerHeight;


    if(posticionObj1 < tamañoDePantalla){
        animacion.style.animation="mover-card1 1.5s ease-out"
    }

})


window.addEventListener("scroll", function(){

    let animacion =this.document.getElementById("cardsIMG3");
    let posticionObj1= animacion.getBoundingClientRect().top;
    console.log(posticionObj1);
    let tamañoDePantalla= window.innerHeight;


    if(posticionObj1 < tamañoDePantalla){
        animacion.style.animation="mover-card1 1.5s ease-out"
    }

})

window.addEventListener("scroll", function(){

    let animacion =this.document.getElementById("cardsIMG4");
    let posticionObj1= animacion.getBoundingClientRect().top;
    console.log(posticionObj1);
    let tamañoDePantalla= window.innerHeight;


    if(posticionObj1 < tamañoDePantalla){
        animacion.style.animation="mover-card1 1s ease-out"
    }

})


